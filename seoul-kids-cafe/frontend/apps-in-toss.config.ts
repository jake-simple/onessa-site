import {defineConfig} from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "kids-cafe-finder",
  brand: {
    primaryColor: "#3182F6"
  },
  permissions: [],
  webBundleDir: "toss-dist"
});
